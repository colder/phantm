package phpanalysis.controlflow

import Types._
import parser.Trees._
import analyzer.Symbols._

object InternalFunctions {
    def lookup(id: Identifier): Option[FunctionSymbol] = {
        None
    }
}
